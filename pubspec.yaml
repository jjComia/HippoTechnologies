name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.0.0
  http: ^1.2.2
  dio: ^5.0.0 # Add Dio for making HTTP requests
  cookie_jar: ^2.0.0 # Add cookie_jar for cookie management
  shared_preferences: ^2.3.2
  flutter_speed_dial: ^6.0.0
  awesome_dialog: ^3.2.1
  workmanager: ^0.5.0 # Added workmanager package

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  

  flutter_launcher_icons: ^0.13.1
  
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/HippoTechnologiesLogo.jpg"


flutter:
  uses-material-design: true
  assets:
    - assets/images/HippoTechnologiesLogo.png